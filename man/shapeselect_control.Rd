% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapeselect.R
\name{shapeselect_control}
\alias{shapeselect_control}
\title{Control parameters for \code{shapeselect()}}
\usage{
shapeselect_control(
  knots_lst = NULL,
  boundary.knots = NULL,
  space = "E",
  nsim = 200,
  nfolds = 5,
  eps = 1e-08,
  pnt = TRUE,
  multicore = getOption("cgam.parallel"),
  a = NULL,
  maxedf = FALSE,
  add_edfu = 0,
  add_kts = NULL,
  shrinketa = FALSE,
  catnrep = FALSE,
  standardize = TRUE,
  center = TRUE,
  verbose = FALSE,
  set4 = TRUE
)
}
\arguments{
\item{knots_lst}{Optional list specifying knots for spline basis functions,
one element per predictor. Use \code{NULL} to let \code{shapeselect()} (and its
helper routines) choose defaults.}

\item{boundary.knots}{Optional list specifiying knots for numeric predictors.}

\item{space}{Character string indicating the knots being equally spaced for spline
bases in underlying fitting routines (e.g., \code{"E"}).}

\item{nsim}{Integer; number of simulations used  when estimating
mean degrees of freedom terms used in variable and shape selection criteria CBIC.}

\item{nfolds}{Integer; number of folds used for cross-validation when
\code{type = "classo"} (shrinkage selection). It is not in use for now.}

\item{eps}{Numeric tolerance used for detecting near-zero signal.}

\item{pnt}{Logical; whether to use penalized spline estimator.}

\item{multicore}{Logical/integer; parallel setting passed to \code{shapeselect()} via the
\code{parallel} control entry. Defaults to \code{getOption("cgam.parallel")}.}

\item{a}{Optional numeric; calibration constant used in the modified CBIC penalty term.
If \code{NULL}, \code{shapeselect()} chooses a value based on \code{n}, \code{p}, and the implied
total number of basis functions.}

\item{maxedf}{Logical; if \code{TRUE}, enforce an effective degrees of freedom cap in internal
forward-step routines (as implemented).}

\item{add_edfu}{Numeric; optional additive adjustment to the unconstrained EDF used in the
information criterion calculations.}

\item{add_kts}{Integer; optional adjustment to the number of knots used in spline bases.
If \code{NULL}, \code{shapeselect()} chooses a default depending on \code{pnt}.}

\item{shrinketa}{Logical; if \code{TRUE} and \code{type = "classo"}, shrink fitted component
contributions by the selected shrinkage parameters. It is not in use for now.}

\item{catnrep}{Logical; if \code{TRUE}, print the iteration counter during the selection path.}

\item{standardize}{Logical; if \code{TRUE}, standardize numeric predictors by their standard deviations.}

\item{center}{Logical; if \code{TRUE}, center numeric predictors when scaling them.}

\item{verbose}{Logical; if \code{TRUE}, print the model-path tracking matrix as the algorithm runs.}

\item{set4}{Logical; optional flag passed through to internal routines used to restrict or
modify the candidate shape set to be increasing, decreasing, convex and concave; if false, then
combinations of monotonicity and convexity will be added}
}
\value{
A named list suitable for the \code{control} argument of \code{shapeselect()}.
}
\description{
Creates a list of parameters used by \code{shapeselect()} to control
basis construction, information-criterion calibration, simulation settings,
parallel computing etc.
}
\examples{
ctrl <- shapeselect_control(nsim = 100, nfolds = 10, verbose = TRUE)
ctrl

}
